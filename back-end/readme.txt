change   application-test.yml file’s data.
change redis, mysql, mail’s config to your own.
and then using mvn install build the project get the project’s jar.
then using 
nohup java -jar “jar’s location and name” &
run the project

IP:8086/swagger-ui.html is the api website
